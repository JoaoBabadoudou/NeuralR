[
  {
    "objectID": "index1.html",
    "href": "index1.html",
    "title": "Hands-on",
    "section": "",
    "text": "Let’s start with an example.\nLet’s suppose that we have two kind of plant that we have to divide into two classes: toxic plants represented by class 0 and healthy plants represented by class 1. Each plant has his characteristics X1 and X2. The following table show us what it is about:\n\n\n\nY\nX1\nX2\n\n\n\n\n1\n0.5\n2\n\n\n1\n1.1\n2.1\n\n\n1\n0.7\n2.6\n\n\n1\n2\n1\n\n\n1\n2.5\n0.7\n\n\n1\n2.2\n0.3\n\n\n\nLet’ us have a better look with a graphic\n\nlibrary(ggplot2)\nx1= c(0.5,1.1,0.7,2,2.5,2.2)\nx2=c(2,2.1,2.6,1,0.7,0.3)\ny= factor(c(1,1,1,0,0,0), levels=c(0,1))\nY=factor(y)\ndata= data.frame(cbind(y,x1,x2)  ) \nggplot(data = data, aes(x = x1, y = x2)) +\n  geom_point(aes(colour = Y), size = 3) + \n  scale_colour_manual(values = c(\"red\", \"green\")) +\n  theme_minimal()\n\n\n\n\n\n\n\n\nAs we can see, we can provide a line that can separate the two kinds of plant. This line is the decision boundary. So we’re able to develop a model, based on this line to predict the class of a new plant.\n\nlibrary(ggplot2)\nx1= c(0.5,1.1,0.7,2,2.5,2.2,1.5)\nx2=c(2,2.1,2.6,1,0.7,0.3,0.5)\ny= factor(c(1,1,1,0,0,0,2), levels=c(0,1,2))\nY=factor(y)\ndata= data.frame(cbind(y,x1,x2)  ) \nggplot(data = data, aes(x = x1, y = x2)) +\n  geom_point(aes(colour = Y), size = 3) + \n  scale_colour_manual(values = c(\"red\", \"green\", \"blue\")) + \n  geom_abline(slope = 1, intercept = 0.1, colour = \"black\", size = 1.2) + # Boundary line\n  labs(title = \"Data separation\", x = \"x1\", y = \"x2\") + \n  theme_minimal()\n\n\n\n\n\n\n\n\nTo de velop the model, we need to know the equation of the line that separate accuratly the two plants. To achieve this, we’ll develop a linear model by sending X1 and X2 to a neuron, adding a coefficient W, respectively W1 and W2 and what we call the bias b. It’ll lead us to :\n\\[  z(X1,X2)= W1*X1 + W2*X2 + b   \\]\nThe boundary line is the value for which \\[ Z(X_1,X_2)=0\\] So by posing \\(Z(X_1,X_2)=0\\), we can find the expression of the equation of the boundary line. To predict a class for a new plant, we have to set the parameters $ W_1 $, $ W_2 $ and \\(b\\) to separe in the best way the two class and after that we will be able to send a new plant according to the sign of the \\(Z\\) computed. Now, to improve this model, one of the best thing that we can do is to add probabilities. The further the plant is from the line, the greater the chance that it belongs to this class. In this context, we use an activation function that returns a value close to 0 or 1 as we move away from the line. Generally we use the sigmoiid fonction.\nThe sigmoid function is one of the most common activation functions in artificial neural networks, especially in simpler or shallow models. It maps any real-valued number to a range between 0 and 1, making it particularly useful for binary classification problems.\nThe sigmoid function \\(a(Z)\\) is defined as:\n\\(a(Z) = \\frac{1}{1 + e^{-Z}}\\)\nWhere:\n\n\\(e\\) is the base of the natural logarithm (approximately 2.718).\n\\(Z\\) is the input to the function, which can be any real number.\n\nThe graph of the sigmoid function has an S-shape (sigmoid curve) and is asymptotic to both 0 and 1 for extreme values of ( x ).\n\nlibrary(ggplot2)\n# Define sigmoid function\nsigmoid &lt;- function(x) {\n  return(1 / (1 + exp(-x)))\n}\nx_values &lt;- seq(-10, 10, by = 0.1)\ny_values &lt;- sigmoid(x_values)\n# Create a data frame for plotting\ndata &lt;- data.frame(x = x_values, y = y_values)\n\n# Plot the sigmoid curve with color differentiation for both the curve and area under it\nggplot(data, aes(x = x, y = y)) +\n  geom_line(size = 1.5, color = \"black\") +  # Black line for sigmoid curve\n  geom_ribbon(data = subset(data, x &lt; 5), aes(ymin = y, ymax = 1), fill = \"lightblue\", alpha = 0.5) +  # Light blue for the left part under and on the curve\n  geom_ribbon(data = subset(data, x &gt;=-5 ), aes(ymin = 0, ymax = y), fill = \"lightcoral\", alpha = 0.5) +  # Light red for the right part under the curve\n  labs(title = \"Sigmoid Function \", x = \"x\", y = \"Sigmoid(x)\") +\n  theme_minimal() +\n  theme(axis.title = element_text(size = 12), plot.title = element_text(size = 14, hjust = 0.5))"
  },
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About",
    "section": "",
    "text": "This site presents the perceptron used as the basis for artificial neural networks. We look in detail at how it’s formed and some of the mathematical concepts behind it."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "The perceptron",
    "section": "",
    "text": "The perceptron is a foundational building block in the fields of artificial intelligence and machine learning. First introduced by Frank Rosenblatt in 1958, it represents one of the earliest and simplest models for supervised learning in neural networks. The perceptron is inspired by the functioning of biological neurons, using mathematical computations to categorize data into one of two distinct classes. Though limited in its ability to handle only linearly separable data, this model has had a lasting impact, paving the way for more advanced architectures, such as multi-layer perceptrons, which can address non-linear problems and are widely used today.\nAt its essence, the perceptron functions as a basic decision-making mechanism. It receives input signals—representing features of the data—applies weights to these inputs, calculates their weighted sum, and processes this sum through an activation function. If the result surpasses a predefined threshold, the perceptron “fires,” producing an output of 1; otherwise, it outputs 0. This characteristic makes the perceptron particularly suitable for binary classification tasks.\nTo better understand the perceptron, let’s consider building a simple neural network in R, starting with this fundamental unit. We’ll also review some essential mathematical concepts as we proceed.\nExample: Classifying Plants\nImagine a scenario where we have two types of plants that need to be classified into two categories:\n- Toxic plants, represented by class 0\n- Healthy plants, represented by class 1\nEach plant is characterized by two features, ( X_1 ) and ( X_2 ). The following table illustrates the dataset for this classification task:\n\n\n\n\n\n\n\n\n\nPlant\n( X_1 ) (Feature 1)\n( X_2 ) (Feature 2)\nClass\n\n\n\n\nPlant 1\nValue\nValue\n0\n\n\nPlant 2\nValue\nValue\n1\n\n\n…\n…\n…\n…\n\n\n\nUsing this dataset, we can develop and train a perceptron to distinguish between the two classes of plants based on their characteristics. This process will involve defining weights, thresholds, and iteratively updating these parameters to improve classification accuracy."
  },
  {
    "objectID": "index.html#mathematical-definition",
    "href": "index.html#mathematical-definition",
    "title": "The perceptron",
    "section": "Mathematical Definition",
    "text": "Mathematical Definition\nThe sigmoid function ( (x) ) is defined as:\n[ (x) = ]\nWhere: - ( e ) is the base of the natural logarithm (approximately 2.718). - ( x ) is the input to the function, which can be any real number.\n\nGraphical Representation\nThe graph of the sigmoid function has an S-shape (sigmoid curve) and is asymptotic to both 0 and 1 for extreme values of ( x )."
  },
  {
    "objectID": "index.html#properties-of-the-sigmoid-function",
    "href": "index.html#properties-of-the-sigmoid-function",
    "title": "The perceptron",
    "section": "Properties of the Sigmoid Function",
    "text": "Properties of the Sigmoid Function\n\nRange: The output is always between 0 and 1, making it ideal for tasks requiring probabilistic interpretation (e.g., binary classification).\nS-shape Curve: The function produces an S-shaped curve, making it useful in logistic regression.\nDifferentiable: The sigmoid function is smooth and differentiable everywhere, which is crucial for optimization techniques like gradient descent.\nNon-linearity: Introduces non-linearity, allowing neural networks to learn complex patterns.\nSymmetry: The function is symmetric around 0, meaning that ( (-x) = 1 - (x) )."
  },
  {
    "objectID": "index.html#advantages-of-the-sigmoid-activation",
    "href": "index.html#advantages-of-the-sigmoid-activation",
    "title": "The perceptron",
    "section": "Advantages of the Sigmoid Activation",
    "text": "Advantages of the Sigmoid Activation\n\nSimplicity: The sigmoid function is mathematically simple and computationally inexpensive to calculate.\nProbabilistic Output: Its output can be interpreted as a probability, which makes it suitable for binary classification tasks."
  },
  {
    "objectID": "index.html#limitations-of-the-sigmoid-activation",
    "href": "index.html#limitations-of-the-sigmoid-activation",
    "title": "The perceptron",
    "section": "Limitations of the Sigmoid Activation",
    "text": "Limitations of the Sigmoid Activation\n\nVanishing Gradient Problem: For extreme input values (both positive and negative), the gradient of the sigmoid function becomes very small, slowing down the learning process. This is called the vanishing gradient problem.\nNot Zero-Centered: The sigmoid function outputs values between 0 and 1, meaning that gradients can be either positive or negative. This can slow down convergence in some cases.\nInefficient for Deep Networks: The sigmoid function is less effective in deep networks due to the vanishing gradient problem, making other functions like ReLU more popular in deeper networks."
  },
  {
    "objectID": "index.html#usage-in-neural-networks",
    "href": "index.html#usage-in-neural-networks",
    "title": "The perceptron",
    "section": "Usage in Neural Networks",
    "text": "Usage in Neural Networks\nIn a neural network, the sigmoid function is applied to the weighted sum of the inputs to a neuron:\n[ y = (w_1 x_1 + w_2 x_2 + + w_n x_n + b) ]\nWhere: - ( w_1, w_2, , w_n ) are the weights. - ( x_1, x_2, , x_n ) are the inputs. - ( b ) is the bias term.\nThe output ( y ) is a value between 0 and 1, which can be interpreted as the probability of the positive class in a binary classification task.\nThe sigmoid function is one of the most widely used activation functions in artificial neural networks, especially in early neural network models. It maps any real-valued number to a range between 0 and 1, which makes it useful for binary classification tasks, where the output needs to be interpreted as a probability.\nMathematical Definition: The sigmoid function 𝜎 ( 𝑥 ) σ(x) is defined as:\n𝜎 ( 𝑥 ) = 1 1 + 𝑒 − 𝑥 σ(x)= 1+e −x\n1 ​\nWhere:\n𝑒 e is the base of the natural logarithm (approximately 2.718). 𝑥 x is the input to the function. Properties of the Sigmoid Function: Range: The output of the sigmoid function lies between 0 and 1. This makes it useful for models where outputs represent probabilities, as the function maps large input values to values close to 1 and small input values to values close to 0.\nS-shape Curve: The sigmoid function has an S-shaped curve, also known as a “logistic curve.” This characteristic is why it is often used in logistic regression.\nDifferentiability: The sigmoid function is smooth and differentiable everywhere, which is essential for gradient-based optimization methods like backpropagation in neural networks.\nNon-linearity: The sigmoid function introduces non-linearity to the model, enabling neural networks to learn complex patterns.\nSymmetry: The sigmoid function is symmetric around 0, meaning that 𝜎(−𝑥)=1−𝜎(𝑥) σ(−x)=1−σ(x)."
  }
]